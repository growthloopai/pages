# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository

This is a static HTML project with no build system, dependencies, or test framework at this stage. Open `index.html` directly in a browser to preview it.

## Project Structure

```
growthloopai/
├── assets/
│   ├── css/          ← stylesheets (style.css, etc.)
│   ├── js/           ← scripts (main.js, etc.)
│   └── images/       ← image assets
├── index.html        ← entry point (stays at root)
├── README.md
└── CLAUDE.md
```
